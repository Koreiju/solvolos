# Phase 6: Multi-Node Retrieval UI - UAT Checklist

## Architecture Constraints

- [ ] pending: **UAT-UI-04**: Playwright MUST verify the forward-slash (`/`) operator triggers the hover-linked secondary billboard, and clicking pins it (Maps to `TDD-UI-04`).
- [ ] pending: **UAT-UI-05**: Playwright MUST verify that rapidly hovering over 10 semantic results in 1 second applies a 150ms debounce/abort controller, rendering only the final secondary panel and preventing DOM spam (Maps to `TDD-UI-05`).
